'use strict';

var gutil = require('gulp-util');//,
    //npmConfig = require('config');

module.exports = {
    paths: {
        src: './src',
        bower: './bower_components',
        temp: './.tmp',
        dest: './dist',
        destWeb: './dist/web'
    }
};